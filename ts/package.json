{
  "name": "functions",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "start": "functions-framework --target=expressApi",
    "prestart": "npm run build",
    "gcp-build": "npm run build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@google-cloud/functions-framework": "^3.1.2"
  },
  "devDependencies": {
    "typescript": "^4.9.3"
  }
}
